# 🚀 Быстрая шпаргалка: Скрапирование 20 страниц

## Локально

```bash
# 1 команда - готово!
python scripts/bulk_scrape_railway.py --pages 20
```

Логи → `bulk_scrape.log`

---

## На Railway

### Способ 1: Railway CLI (самый простой)

```bash
npm i -g @railway/cli
railway login
railway link
railway run python scripts/bulk_scrape_railway.py --pages 20
```

### Способ 2: GitHub + Railway автоматически

```bash
git push origin main
# Railway автоматически пушит изменения
```

Потом запустить через Dashboard или CLI.

### Способ 3: Railway Dashboard

1. https://railway.app/dashboard
2. Нажимаем на Backend сервис
3. Выполняем SSH команду или используем Railway CLI

---

## Результаты

```
📊 СТАТИСТИКА ПО ИСТОЧНИКАМ:
   CASA_IT      → ~150 объявлений
   SUBITO       → ~200 объявлений
   IDEALISTA    → ~170 объявлений
   IMMOBILIARE  → ~200 объявлений

💾 В БД сохранено: ~700 новых объявлений
⏱️ Время: 30-40 минут
```

---

## Параметры

```bash
--pages 10    # 10 страниц (15-20 мин)
--pages 20    # 20 страниц (30-40 мин) - default
--pages 50    # 50 страниц (1.5-2 часа)
```

---

📖 Подробное руководство: `docs/BULK_SCRAPE_GUIDE.md`
