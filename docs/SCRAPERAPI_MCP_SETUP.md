# Настройка ScraperAPI MCP Server

## ✅ Что уже сделано

1. ✅ Установлен пакет `scraperapi-mcp-server==0.2.0` в виртуальное окружение `.venv`
2. ✅ Пакет добавлен в `requirements.txt`

## 📋 Требования

- Python 3.11+ (у вас установлен Python 3.11.9 ✅)
- ScraperAPI API ключ (получите на [scraperapi.com](https://scraperapi.com))

## 🔧 Настройка для Cursor

### Шаг 1: Получите API ключ ScraperAPI

1. Зарегистрируйтесь на [scraperapi.com](https://scraperapi.com)
2. Получите API ключ из Dashboard
3. Добавьте ключ в `.env` файл:

```bash
SCRAPERAPI_KEY=your-scraperapi-api-key-here
```

### Шаг 2: Настройка MCP сервера в Cursor

В Cursor настройка MCP сервера происходит через файл конфигурации. Создайте или отредактируйте файл конфигурации MCP:

**Для macOS (Cursor конфигурация):**

Путь к конфигурации обычно: `~/Library/Application Support/Cursor/User/globalStorage/mcp.json`

Или настройте через Settings → MCP Servers в Cursor.

**Пример конфигурации:**

```json
{
  "mcpServers": {
    "ScraperAPI": {
      "command": "python",
      "args": ["-m", "scraperapi_mcp_server"],
      "env": {
        "API_KEY": "your-scraperapi-api-key-here"
      },
      "cwd": "/Users/aleksandrbaranov/Documents/Work/MY_CODE_PROJECTS/ITA_RENT_02"
    }
  }
}
```

**Альтернативный вариант с использованием виртуального окружения:**

```json
{
  "mcpServers": {
    "ScraperAPI": {
      "command": "/Users/aleksandrbaranov/Documents/Work/MY_CODE_PROJECTS/ITA_RENT_02/.venv/bin/python",
      "args": ["-m", "scraperapi_mcp_server"],
      "env": {
        "API_KEY": "your-scraperapi-api-key-here"
      }
    }
  }
}
```

### Шаг 3: Проверка установки

После настройки MCP сервера, вы можете использовать ключевое слово `scrape` в промптах Cursor, и LLM автоматически будет использовать ScraperAPI для получения данных.

Например:

- "scrape данные с сайта..."
- "используя scrape, получи информацию о..."

## 🐳 Альтернатива: Использование Docker

Если предпочитаете использовать Docker:

```json
{
  "mcpServers": {
    "ScraperAPI": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "-e",
        "API_KEY=your-scraperapi-api-key-here",
        "--rm",
        "scraperapi-mcp-server"
      ]
    }
  }
}
```

## 📝 Примечания

1. **Безопасность**: Никогда не коммитьте API ключи в git. Используйте `.env` файл (который уже в `.gitignore`)

2. **Переменные окружения**: MCP сервер ожидает переменную `API_KEY`, но в вашем проекте используется `SCRAPERAPI_KEY`. При настройке MCP используйте `API_KEY` в конфигурации.

3. **Локальная разработка**: Если вы используете MCP только для локальной разработки, можете указать путь к виртуальному окружению проекта напрямую.

## 🔗 Дополнительные ресурсы

- [Официальная документация ScraperAPI MCP](https://docs.scraperapi.com/mcp-server/introduction-to-mcp-server/installation)
- [ScraperAPI Dashboard](https://dashboard.scraperapi.com/)

## ❓ Решение проблем

### Ошибка: "API_KEY environment variable is not set"

**Решение**: Убедитесь, что в конфигурации MCP указана переменная `API_KEY` в секции `env`.

### Ошибка: "Python module not found"

**Решение**: Убедитесь, что используете правильный путь к Python из виртуального окружения или установите пакет глобально.

### MCP сервер не отвечает

**Решение**:

1. Проверьте, что API ключ действителен
2. Перезапустите Cursor после изменения конфигурации
3. Проверьте логи Cursor на наличие ошибок
